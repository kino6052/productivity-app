// Fixed for now -- making these configurable per item is a facet extension
// for later (docs/checklist.md), not needed by any test yet.
export const WORK_DURATION_SECONDS = 25 * 60;
export const BREAK_DURATION_SECONDS = 5 * 60;
